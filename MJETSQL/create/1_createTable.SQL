--
USE spotlight;
-- 주의사항 : 보수적으로 늘릴 컬럼
-- 헤더적요 : 255
-- 전표적요 : 255
-- 로컬금액 : 30 : BIGINT(30) 고려 => 매우 중요
-- 거래금액 : 30 : DOUBLE() 고려

CREATE TABLE sy2312 -- FROM TOOL
( ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
`MANDT` VARCHAR(3),
`BUKRS` VARCHAR(6),
`GJAHR` VARCHAR(6),
`BELNR` VARCHAR(12),
`BUZEI` VARCHAR(5),
`SHKZG` VARCHAR(4),
`WRBTR` VARCHAR(19),
`DMBTR` BIGINT(30),
`WAERS` VARCHAR(4),
`HKONT` VARCHAR(9),
`SGTXT` VARCHAR(45),
`BLART` VARCHAR(4),
`BLDAT` VARCHAR(10),
`BUDAT` VARCHAR(10),
`CPUDT` VARCHAR(10),
`USNAM` VARCHAR(5),
`XSTOV` VARCHAR(4),
`BKTXT` VARCHAR(25),
INDEX id1(
    ID,
    `BELNR`,
    `HKONT`)
);

